const guards = [
    "Hank the tank",
    "Thomas the tank engine",
    "CEPUS"
];

function addGuards(guard){
    guards.push(guard);
    return guards;
};

export default {
    guards,
    addGuards
}

